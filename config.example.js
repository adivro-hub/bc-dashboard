/* Copy this file to `config.js` and fill in your Supabase project values.
 * `config.js` is gitignored — your URL + anon key are kept out of the repo.
 * The anon key is meant to be public (security comes from RLS in Postgres),
 * so storing it in client-side code is the intended pattern.
 */
window.BC_CONFIG = {
  SUPABASE_URL:      "https://YOUR-PROJECT-ref.supabase.co",
  SUPABASE_ANON_KEY: "PASTE_YOUR_ANON_KEY_HERE",
};
